# The two managed nodes attached to your session.
#
# They are throwaway containers that exist only while this lab session does,
# and they are reachable from your control node and from nowhere else.
[web]
node1
node2
