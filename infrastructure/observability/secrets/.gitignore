# The scrape token is a credential and never belongs in git.
# `make setup` writes it here from .env; the directory itself is tracked so the
# bind mount has something to attach to on a fresh clone.
scrape-token
