# A notification destination is usually a credential (a chat webhook URL embeds
# its token) and never belongs in git. The directory itself is tracked so the
# bind mount has something to attach to on a fresh clone.
*
!.gitignore
!README.md
